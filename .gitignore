# IDE
.idea/
.vscode/

# OS
.DS_Store

# Claude per-project state
.claude/
skills/
CLAUDE.md
AGENT.md

# Rust
target
**/*.rs.bk
# Cargo.lock IS committed: this is a binary crate, and the lock file
# is what makes a build reproducible against the exact ratatui /
# reqwest / tokio versions it was tested with.

# Python / uv (uv.lock IS committed: it's the pinned environment the
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Project-specific
.env
output/*

# Misc
*.log

# Keep .gitkeep files
!/**/.gitkeep

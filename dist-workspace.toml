[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# The preferred Rust toolchain to use in CI (rustup toolchain syntax)
rust-toolchain-version = "stable"
# CI backends to support
ci = "github"
# release.yml is hand-edited: its actions are pinned to commit SHAs and its
# permissions are narrowed per job. Without this, `dist plan` fails the release
# with "has out of date contents". The tradeoff is that dist no longer refreshes
# the file either, so a dist upgrade means regenerating it deliberately and
# re-applying both changes.
allow-dirty = ["ci"]
# The installers to generate for each app
installers = ["homebrew"]
# A GitHub repo to push Homebrew formulas to
tap = "jrenno-kantic/homebrew-tap"
# Customize the Homebrew formula name
formula = "herd-llm"
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu"]
# Publish jobs to run in CI
publish-jobs = ["homebrew"]
# Whether to publish prereleases to package managers
publish-prereleases = false

# Commands HERD executes at runtime. These become dependencies of the
# generated Homebrew formula rather than build-machine-only packages.
[dist.dependencies.homebrew]
hf = { stage = ["run"] }
"llama.cpp" = { stage = ["run"] }
